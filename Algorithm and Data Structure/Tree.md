# 01 Tree

## 1 Traverse
- preorder
```
def preorder(root) : 
    return [root.val] + self.preorder(root.left) + self.preorder(root.right) if root else []

def preorder(root) : 

```
- inorder
- postorder
